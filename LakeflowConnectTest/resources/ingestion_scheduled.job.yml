resources:
  jobs:
    ingestion_pipe_job:
      name: ingestion_pipe job
      trigger:
        pause_status: UNPAUSED
        periodic:
          interval: 1
          unit: WEEKS
      tasks:
        - task_key: ingestion_pipe_job
          pipeline_task:
            pipeline_id: ${resources.pipelines.pipeline_ingestion_pipe.id}